// assigning variable using let
// with using or assigning variable with let keyword we can reassign the value

let val1 = 'hello';
val1 = 'bye';     // remember here we can reassign the same type only
// with using or assigning variable with const keyword we cannot redecalcare
const val3  = 'qwerty';
const val3 = 'dddd';    // here we cannot reassign the value of same type even

